const jwt = require('jsonwebtoken');
const validate = (token, cb)=>{

}
const deJWT = (token, cb) =>{}
const create = (cb)=>{}

module.exports = {
    validate: validate,
    create: create,
    deJWT: deJWT
}